<form>
  <label class="contained-target-inline">
    {{#if description}}
      {{description}}
    {{else}}
      {{localize "wv.labels.rolls.genericModifierDescription"}}
    {{/if}}
    <input class="size-3" name="modifier" type="number" value="0"{{#if max}} max="{{max}}"{{/if}}{{#if min}} min="{{min}}"{{/if}} />
  </label>
  <button>{{localize "wv.labels.rolls.roll"}}</button>
</form>
