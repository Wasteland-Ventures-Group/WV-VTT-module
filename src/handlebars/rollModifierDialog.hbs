<form>
  <label>
    {{#if description}}
      {{description}}
    {{else}}
      {{localize "wv.labels.rolls.genericModifierDescription"}}
    {{/if}}
    <input name="modifier" type="number" value="0" />
  </label>
  <button class="rollable">{{localize "wv.labels.rolls.roll"}}</button>
</form>
