// import registerTestBatches from "../../integrations/quench/registerBatches.js";

/** Register system callbacks for the Quench module ready hook. */
/*
export default function registerForQuenchReady(): void {
  Hooks.once("quenchReady", registerTestBatches);
}

export type HookParams = Parameters<Hooks.StaticCallbacks["quenchReady"]>;

*/
